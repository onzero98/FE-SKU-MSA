import { useEffect, useRef } from 'react';

export default function Part2(props) {
  const { styles } = props;
  return (
    <section id='Part2' className={styles.Part2}>
      2
    </section>
  );
}
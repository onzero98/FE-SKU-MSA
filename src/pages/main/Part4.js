import { useEffect, useRef } from 'react';

export default function Part4(props) {
  const { styles } = props;
  return (
    <section id='Part4' className={styles.Part4}>
      2
    </section>
  );
}